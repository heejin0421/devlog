---
title: Next.js 에서 KakaoMap 사용하기
date: 2023-03-16
description: KakaoMap 사용하기
---

# KakaoMap API 연결하기

카카오 개발자 페이지에 접속
https://developers.kakao.com/

## 전체코드

```javascript
import React, { useEffect } from 'react';

const ContentsMap = () => {
  useEffect(() => {
    const script = document.createElement('script');
    script.src = `https://dapi.kakao.com/v2/maps/sdk.js?appkey=${process.env.NEXT_PUBLIC_KAKAO_MAP_KEY}&libraries=services&autoload=false`;
    document.head.appendChild(script);

    script.onload = () => {
      window.kakao.maps.load(function () {
        const container = document.getElementById('map');

        const options = {
          center: new kakao.maps.LatLng(33.450701, 126.570667), //지도의 중심좌표.
          level: 3, //지도의 레벨(확대, 축소 정도)
        };

        new kakao.maps.Map(container, options);
      });
    };
  }, []);

  return <div id='map' style={{ width: '100%', height: 312 }} />;
};

export default ContentsMap;
```
