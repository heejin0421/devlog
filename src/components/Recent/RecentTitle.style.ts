import styled from '@emotion/styled';

export const RecentTitleContainer = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;
  max-width: 720px;
  margin-top: 50px;
  /* background-color: blue; */
`;

export const RecentTitleText = styled.div`
  font-size: 28px;
  font-weight: 500;
`;

export const PostMore = styled.div`
  font-size: 16px;
  font-weight: 500;
  color: var(--accent);
`;
