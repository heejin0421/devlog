import styled from '@emotion/styled';

export const TitleContainer = styled.div`
  margin-bottom: 32px;

  h1 {
    font-size: 30px;
    font-weight: 600;
  }

  p {
    font-size: 18px;
    color: var(--text-base-90);
  }
`;
